# Python bytecode (bridge node)
__pycache__/
*.pyc

# Node
node_modules/

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
*.log
*.swp

# Local secrets / env
.env
.env.local
.env.*.local

# Never commit DSH profile data (may contain tokens / local config)
.dsh/

# Internal development plans / handover docs — kept local only, never pushed
# to the public repo or shipped in the npm package.
docs/PLAN-*.md
docs/NEXT-VERSION-PLAN.md

# local emergency backups
*.bak-*
